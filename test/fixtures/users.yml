# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

# This model initially had no columns defined. If you add columns to the
# model remove the "{}" from the fixture names and add the columns immediately
# below each fixture, per the syntax in the comments below
#
max:
  email: maximilian@konow.ski
  encrypted_password: <%= Devise::Encryptor.digest(User, '3xtremely_secure!') %>

eline:
  email: eline@hanna-solutions.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'also_3xtremely_secure!') %>

some_rich_dude:
  email: some@dude.rich
  encrypted_password: <%= Devise::Encryptor.digest(User, 'Password_123!') %>
